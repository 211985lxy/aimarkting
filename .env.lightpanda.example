# Lightpanda 无头浏览器配置
# Lightpanda CDP 端点（默认使用本地 Docker 容器）
LIGHTPANDA_CDP_ENDPOINT=ws://127.0.0.1:9223

# 禁用 Lightpanda 遥测数据收集
LIGHTPANDA_DISABLE_TELEMETRY=true

# Playwright 浏览器配置（可选，如果 sau 使用 Playwright）
PLAYWRIGHT_CHROMIUM_URL=ws://127.0.0.1:9223
